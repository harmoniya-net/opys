{
  "name": "@opys/cli",
  "version": "0.0.2",
  "bin": {
    "opys": "dist/opys.mjs"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown --config tsdown.config.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run tests/unit --passWithNoTests",
    "test:int": "vitest run tests/integration --testTimeout=180000"
  },
  "type": "module",
  "dependencies": {
    "@opys/core": "^0.0.2",
    "@opys/dev": "^0.0.2",
    "@opys/runtime": "^0.0.2",
    "@opys/minecraft": "^0.0.2",
    "@opys/mojang": "^0.0.2"
  },
  "peerDependencies": {
    "zod": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
