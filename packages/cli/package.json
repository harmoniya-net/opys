{
  "name": "@opys/cli",
  "version": "1.0.0",
  "bin": {
    "opys": "dist/opys.mjs"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown --config tsdown.config.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run tests/unit --passWithNoTests",
    "test:int": "vitest run tests/integration --testTimeout=180000"
  },
  "type": "module",
  "dependencies": {
    "@opys/core": "^1.0.0",
    "@opys/dev": "^1.0.0",
    "@opys/runtime": "^1.0.0",
    "@opys/minecraft": "^1.0.0",
    "@opys/mojang": "^1.0.0"
  },
  "peerDependencies": {
    "zod": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
