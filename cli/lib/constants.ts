/** Default maximum parallel downloads for the install command. */
export const DEFAULT_CONCURRENCY = 8;

/** Width in characters of the progress bar block. */
export const BAR_WIDTH = 24;

/** Minimum interval between progress line emissions on non-TTY streams. */
export const NON_TTY_INTERVAL_MS = 3_000;
