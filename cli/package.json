{
  "name": "@lanka/cli",
  "version": "1.0.0",
  "bin": {
    "lanka": "dist/lanka.mjs"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown --config tsdown.config.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run tests/unit --passWithNoTests",
    "test:int": "vitest run tests/integration --testTimeout=180000"
  },
  "type": "module",
  "dependencies": {
    "@lanka/core": "^1.0.0",
    "@lanka/dev": "^1.0.0",
    "@lanka/runtime": "^1.0.0",
    "@lanka/minecraft": "^1.0.0",
    "@lanka/mojang": "^1.0.0"
  },
  "peerDependencies": {
    "zod": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
