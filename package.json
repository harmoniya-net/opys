{
  "name": "unifest",
  "version": "1.0.0",
  "scripts": {
    "format": "prettier --write .",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:int": "npm run test:int --workspaces --if-present",
    "prepare": "husky",
    "bump": "npm version --workspaces --include-workspace-root"
  },
  "workspaces": [
    "rules",
    "core",
    "minecraft",
    "mc",
    "forge",
    "installer",
    "cli"
  ],
  "devDependencies": {
    "@types/node": "^22.19.17",
    "git-format-staged": "^4.0.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.1",
    "tsdown": "^0.20.3",
    "typescript": "^5.8.2",
    "vitest": "^4.1.2"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "lint-staged": {
    "*.{ts,js,md}": "prettier --write"
  }
}
